-- DROP DATABASE IF EXISTS chipindb;
-- CREATE DATABASE chipindb;

-- USE chipindb;

-- CREATE TABLE activity (
--     id Int AUTO_INCREMENT NOT NULL,
--     `title` VARCHAR (200) NOT NULL,
--     `body` VARCHAR (100000) NOT NULL,
--     `contact` VARCHAR (100) NOT NULL,
--     `address` VARCHAR (1000) NOT NULL,
--     `time` VARCHAR (100),
--     `dates` VARCHAR (100) NOT NULL,
--     `dateposted` DATETIME NOT NULL,
--     `hours` VARCHAR (100),
--     `points` VARCHAR (100),
--     PRIMARY KEY (id)
-- );


-- INSERT INTO activity
--     (title, body, contact, address, `time`, dates, dateposted )
-- VALUES('Elders Need Helpers
-- in the Tucson Community, Volunteer to Help them Today!', 'Volunteer
-- to help them for the activities you enjoy, on a schedule that works best for you. Go
-- to ElderHelpers' , 'fakeemail@email.com', 'Tucson, AZ','testing time', 'test dates', '7/7/18');

-- INSERT INTO activity
--     (title, body, contact, address, `time`, dates, dateposted )
-- VALUES('A yearbof service with Habitat AmeriCorps', 'Habitat
-- for Humanity is seeking to fill 400+ AmeriCorps positions in communities across the United States. For one year, you…' , 'fakeemail@email.com', 'Tucson, AZ', 'testing time', 'test dates', '7/7/18');

-- INSERT INTO activity
--     (title, body, contact, address, `time`, dates, dateposted )
-- VALUES('After School Activity
-- Coordinator - Higher Ground - AmeriCorps', 'Higher Ground, A Resource
-- Center After School Activity Coordinator 1700 Hour Position Approximately 40' , 'fakeemail@email.com', 'Tucson, AZ', 'testing time', 'test dates', '7/7/18');

-- INSERT INTO activity
--     (title, body, contact, address, `time`, dates, dateposted )
-- VALUES('Homework Mentor
-- - Higher Ground Resource Center - AmeriCorps', 'Higher Ground, A Resource
-- Center Homework Mentor 300 Hour Position Approximately 8-10 hours/week Service ' , 'fakeemail@email.com', 'Tucson, AZ', 'testing time', 'test dates', '7/7/18');
    
